# Desarrollo local — copia este archivo a .env.local (no se sube al repo)
VITE_API_URL=http://localhost:4000/api
# WebSocket (opcional; por defecto usa el host de VITE_API_URL sin /api)
# VITE_WS_URL=http://localhost:4000

# Producción (configurar en Vercel Environment Variables)
# VITE_API_URL=https://calendar-back-juanda14s-projects.vercel.app/api
